apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: default
  labels:
    app: guess-that-track
    component: backend
spec:
  type: ClusterIP
  selector:
    app: guess-that-track
    component: backend
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
